# С чего продолжить

Состояние на 11.09.2026. Этот файл — для того, чтобы начать работу в новом окне
и не восстанавливать контекст заново.

Рядом лежат: `SETUP.md` — окружение и команды запуска, `НОРМАЛИЗАЦИЯ.md` — что
можно и чего нельзя менять в Excel, `PLAN.md` — старый план, частично устарел.

---

## 1. На чём остановились

**Все семь спорных сделок августа закрыты 11.09.2026** — по слову Николая,
каждая доказана пересчётом платежей в евро по курсу ЦБ на дату платежа против
счетов Bitrix. 14 привязок на 12 768 786,02 ₽, девять сделок сошлись с файлом
до копейки. Открытых вопросов по разноске сейчас нет.

Последним решён Приангарский: на спецификацию 39 (счёт 1239, 7 800 €) пришло
три платежа по 3 900 €. Николай подтвердил, что лишняя половина (#2224) зачтена
в спецификацию 43 — привязан к сделке 907, она сошлась с файлом.

### Как решали (на будущее — метод работает)

Счета в Bitrix выставлены в евро, платежи приходят в рублях. Если перевести
каждый платёж по курсу ЦБ на его дату, он совпадает со счётом до цента — это
сильнее любого текста в назначении. Так выяснилось, что у Павловского спец 4 —
это счёт 3133 (две половины по 11 962,50 €), а не два «авансовых по спец. 4»:
те ровно равны счетам 3059 и 3077 и к этой сделке не относятся.

| Сделка | Платежи | Доказательство |
|---|---|---|
| ПИК ЛЕС, спец 1 | #2411, #119 | = счета 2711 и 3265 до цента |
| Аспэк, спец 14/15/16/17 | #973 и #988, каждый разрезан надвое | суммы в евро в самом назначении |
| С-ДОК, счёт 2881 | #2522, #215, #492 | 6 527,00 € = счёт 2881; «Сокольский ДОК» в Bitrix — тот же ИНН 3527008949 |
| Павловский ДОК, спец 4 | #56, #481 | 23 925,00 € = счёт 3133 |
| Профлес, счёт 3853 | #996 | #1003 — «платёж возвращён клиенту», не привязан |
| Приангарский, спец 43 | #1892, #2224 | 20% от 8 886,24 € + переплата по СП39 (3 900 €), зачёт подтвердил Николай |

Разрезать платёж можно без доработки программы: у каждой привязки в
`payment_allocations` своя сумма.

---

## 2. Что сейчас в базе

| | |
|---|---|
| Сделок | **374**, с разноской **327** |
| Платежей | **2 231** за 04.09.2023 — 07.09.2026, на 727,8 млн ₽; у 67 августовских есть ИНН плательщика |
| Связей «платёж → сделка» | **414** |
| Оплачено по сделкам | 118,8 млн ₽, остаток 157,0 млн ₽ |
| Клиентов | 151 (17 новых с 11.09.2026) |

Работает по расписанию: курсы ЦБ каждое утро (USD, EUR, CNY, история с декабря
2024), счета и компании Bitrix каждый час (валюта, сумма, ИНН, КПП, юрназвание).

---

## 3а. Что сделано 11.09.2026

- **Залиты файлы от 10.09 (платежи) и 05.09 (Buyers).** Платежей +72 (август и
  начало сентября, 12,1 млн ₽), сделок +45, клиентов +17. Ноль сдвигов строк,
  по бэкапу `/tmp/finbg-before-load-2026-09-11.dump` сверено: все 351 прежние
  привязки и все привязки к счетам на месте.
- **Сверено с Bitrix до заливки:** все 35 новых счетов в Bitrix есть; ИНН из
  файла платежей ни разу не разошёлся ни со справочником, ни с компанией счёта.
  «ГФК», «ПТС ХАРДВУД», «СЗ ИНКОСТ», «ТД Беловежский» — те же фирмы, что в файле,
  подтверждено по ИНН.
- **Разноска закрыла ещё 4 счёта на 6,85 млн ₽** — Павловский ДОК 3655,
  Рубцовский ЛДК 3213, Сетново 3609, Галичский 3775.
- **В Bitrix дыра:** счёт 3917 (Эксполес) без привязанной компании, у
  ХОЛЬЦФОРМ (счёт 3875) компания без реквизитов.
- **Третий таймер** — `finbg-bitrix-sync.timer` каждые 15 минут гоняет
  `sync_bitrix_payment_managers.py`; он и наполняет привязки к счетам Bitrix
  (1 233 на 11.09).

---

## 3. Что сделано 24.08.2026

- **Залиты платежи с 01.09.2023** — было 504 за один 2026 год, стало 2 159 за
  три года. Проверка перед заливкой показала ноль сдвигов строк; после заливки
  сверено по бэкапу, что все 304 прежние связи на месте, а 272 ручные привязки
  не тронуты.
- **Начата разноска старых платежей** — `link_payments_by_document.py` привязал
  37 платежей к 16 сделкам на 18,7 млн ₽. Правило: тот же клиент + номер счёта
  или спецификации в назначении + сумма вместе с уже стоящими привязками сходится
  с файлом до копейки.
- **Найдено: триггера пересчёта итогов в живой базе нет.** Миграцию
  `012_deal_totals_from_allocations.sql` не применяли. После любой правки привязок
  «оплачено» и «остаток» надо пересчитывать тем же запросом, что стоит в конце
  `import_buyers_deals.py`, иначе сделка показывает старую цифру.
- **`apply_confirmed_client_aliases.py` работает вхолостую** — ищет сделку по
  `d.paid_amount_rub = p.amount_rub`, а у неразнесённой сделки это ноль. Сверять
  надо с `source_payload->>'paidAmount'`. Не трогали.
- **Выложен обратный ход в Bitrix** — `sync_bitrix_invoice_payments.py` шлёт в
  карточку счёта оплаченное, остаток и стадию. Без `--apply` только показывает,
  в расписании нет.

### Порядок заливки файлов (на будущее)

```bash
# 1. забрать свежие копии
scp "W:/BG/FinanceBattery/PaymentsBatteryRUS_2012.xlsx" VPS:/tmp/Payments.xlsx
scp "W:/BG/FinanceBattery/Buyers/Buyers.xlsx" VPS:/tmp/Buyers.xlsx

# 2. разобрать
ssh VPS "sudo -u finbg /opt/finbg/venv/bin/python /opt/finbg/imports/read_payments_xlsx.py /tmp/Payments.xlsx /tmp/payments_new.json --period-from 2023-09-01"
ssh VPS "sudo -u finbg /opt/finbg/venv/bin/python /opt/finbg/imports/read_buyers_xlsx.py /tmp/Buyers.xlsx /tmp/deals_new.json"

# 3. ПРОВЕРКА ВХОЛОСТУЮ — в базу не пишет ничего, показать Николаю
ssh VPS "sudo -u finbg /opt/finbg/venv/bin/python /opt/finbg/imports/check_payments_reimport.py /tmp/payments_new.json"
ssh VPS "sudo -u finbg /opt/finbg/venv/bin/python /opt/finbg/imports/check_buyers_reimport.py /tmp/deals_new.json"

# 4. снять бэкап, по нему потом сверяется, что связи целы
ssh VPS "sudo -u finbg pg_dump -Fc -d finbg -f /tmp/finbg-before-<что>-<дата>.dump"

# 5. только после «да» — заливка (--source-path обязан совпадать с прошлым!)
#    платежи: import_payment_battery.py → promote_incoming_payments.py
#    сделки:  import_buyers_deals.py → import_buyers_receipts.py
#    прошлые значения --source-path смотреть в таблице import_batches
```
---

## 4. Открытые вопросы

**Валюта спецификаций** — 27 сделок на 42,5 млн ₽. В Bitrix спецификаций нет.
По 8 закрытым сумма в евро вычислима из платежей, по 19 нужны суммы Николая.
Отложено им самим 07.08.2026.

**Кнопка загрузки в интерфейсе** — последний пункт первого этапа. Чтение xlsx
готово, осталось показать результат проверки на экране и дать подтвердить.

**ИНН плательщика** — Николай вносит его в файл платежей с августа 2026. Поле в
базе заведено, загрузчик читает заголовки «ИНН», «ИНН плательщика», «ИНН клиента».

**Обзор наполовину нарисован** — график по месяцам, платёжный календарь и
«Активные проекты» пока выдуманные данные.

**Девять клиентов без ИНН** — четверо частные лица (не нужен), Ozon в Bitrix
назван иначе, трое новых подтянутся сами.

**Три мёртвых файла на сервере** — ничего не делают, но при разборе полётов
собьют с толку. Убрать с бэкапом, когда дойдут руки: `/opt/finbg/api/main.py`
(копия времён до переезда в `app/`; служба запускается через `app.main`),
`/opt/finbg/api/scripts/import_payment_battery.py` (копия от 26.07, грузим
версией из `imports/`) и `sync_bitrix_invoice_payment_status.py` (в репозитории
его нет, похоже предшественник `sync_bitrix_invoice_payments.py`). Всё
остальное — код API, восемь загрузчиков и проверок в `/opt/finbg/imports`,
скрипты обмена с Bitrix — совпадает с репозиторием буква в букву, сверено
24.08.2026.

**Забытая копия библиотек** `web/node_modules.old-2026-07-31` — из-за неё падает
`npm run lint`: проверка лезет внутрь и находит три тысячи чужих замечаний.
Удалить.

**Статус разноски врёт по смыслу** — 23 сделки держат деньги (18,1 млн ₽), но
числятся `unmatched` или `review`. Статус означает «загрузчик нашёл платёж по
дате и сумме из файла», а читается как «на сделке нет денег». Стоит развести
два понятия.

---

## 5. Грабли

**Excel: правка доходит только после сохранения.** Дважды теряли время на
«я исправил» — а на диске старое. Всегда сверять отметку изменения файла.
Закрывать книгу не нужно, достаточно сохранить.

**Платёж опознаётся по пути к файлу, листу и номеру строки.** Вставленная строка
сдвигает всё ниже — проверка сверяет ещё и содержимое строки. В `Buyers` удалять
строки безопасно: сделка опознаётся по документу.

**Паспорт сделки** — покупатель, вид документа, номер, дата документа. Меняется
любое поле — для программы это новая сделка. Название покупателя менять можно:
форма собственности, кавычки и порядок слов отбрасываются.

**Загрузчики никогда не удаляют.** Всё, что надо убрать, убирается отдельно и
только по слову Николая.

**Ручные исключения** живут в `payment_row_exclusions` и `deal_row_exclusions` —
внутри загруженных данных пометка стёрлась бы при первой же заливке.

**«Оплачено» пересчитывается только при загрузке файла.** Ручная привязка или
перенос платежа — не загрузка, пересчёт надо делать тем же запросом.

**Пуш в GitHub делает Николай** — Claude его блокирует. Команда — в `SETUP.md`.
