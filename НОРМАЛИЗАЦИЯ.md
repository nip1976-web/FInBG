# Перечень нормализации таблиц Excel

Что можно менять свободно, чего нельзя касаться, и что нужно добавить перед
повторной загрузкой данных в базу. Состояние на 03.08.2026.

Файлы: `W:\BG\FinanceBattery\Buyers\Buyers.xlsx` (лист `СчетаРуб`) и
`W:\BG\FinanceBattery\PaymentsBatteryRUS_2012 — копия.xlsx` (листы
`РС-Поступ-Хольц`, `ПоступленияН`).

---

## 1. Чего нельзя менять

У каждой сделки есть **паспорт** — по нему программа узнаёт, что строка в файле
и запись в базе относятся к одному и тому же документу. Совпал паспорт — все
привязанные платежи остались на месте. Изменилось хоть одно поле — строка
считается новой сделкой, а старая остаётся в базе с деньгами, но без строки в
файле. Внешне это выглядит как «платежи потерялись».

Паспорт составляют **три поля** листа `СчетаРуб`:

| Поле | Что нельзя делать |
|---|---|
| **Вид документа** | менять написание: `счет` не должно стать `Счёт`, `счёт`, `Счет на оплату` |
| **Номер документа** | добавлять символы: `3089` не должно стать `№3089`, `3089/1`, `00003089` |
| **Дата документа** | сдвигать, менять формат, заполнять пустую или очищать заполненную |

Строки **без номера документа** (Ozon, физлица — 35 сделок) опознаются иначе:
по покупателю, **дате оплаты** и **сумме сделки**. Для них нельзя менять эти два
поля.

> Проверено на живых данных: три типовые правки — переименование покупателя,
> номер `№3213` вместо `3213` и сдвиг даты на день — оборвали 15 сделок,
> 19 платежей, **4 741 132 ₽**.

---

## 2. Что менять можно свободно

**Название покупателя — можно.** Пишите так, как удобно читать менеджерам.
Программа отбрасывает перед сравнением форму собственности, кавычки, знаки
препинания, регистр и порядок слов:

```
Ромашка ООО   =   ООО Ромашка   =   ООО "Ромашка"   =   ромашка ооо
```

Проверено: переформатирование 270 названий из 283 не оборвало ни одной сделки.

Чего правило **не** свяжет — сокращение с полным написанием: `Приангарский ЛПК`
и `Приангарский Лесоперерабатывающий Комплекс`. Для таких случаев ведётся список
альтернативных написаний; сейчас в нём одна запись. Если при нормализации вы
переименуете клиента таким образом — скажите, добавлю.

Также можно свободно:

- переставлять и переименовывать колонки, добавлять новые;
- менять суммы, примечания, менеджера, статус отгрузки;
- удалять и вставлять строки — номер строки больше не является признаком сделки.

---

## 3. Что нужно добавить

### 3.1. В `СчетаРуб` — валюта и сумма в валюте документа

Две колонки:

| Колонка | Что писать |
|---|---|
| **Валюта документа** | `RUB` или `EUR` |
| **Сумма в валюте документа** | для рублёвых — та же сумма; для валютных — сумма в евро |

**Зачем.** Счёт в евро, оплаченный рублями, в рублях не сходится никогда: части
оплачены по разным курсам. В евро сходится копейка в копейку — проверено.
Для счетов валюту уже берём из Bitrix (58 счетов в евро из 217), а вот
**спецификаций в Bitrix нет** — а это 27 сделок на **42,5 млн ₽**, почти половина
всех денег.

Вывести сумму в евро из рублёвой невозможно: в разных строках файла использованы
разные курсы и разная логика. Проверял, отказался.

### 3.2. В файл платежей — ИНН плательщика

Одна колонка: **ИНН плательщика**, если он есть в выписке банка.

**Зачем.** Сейчас плательщик опознаётся по названию. ИНН от написания не зависит
и позволяет поймать случай, когда платит не тот, на кого выставлен счёт, — а
это именно то, что стоит замечать. Сегодня такое ловится сравнением названий,
то есть плохо.

Если в выписке ИНН нет — не страшно, вопрос закроется при подключении
Альфа-Банка напрямую.

---

## 4. Что поправить в самих данных

Известные расхождения. Каждое требует вашего решения — программа их не трогает.

| Что | Где | Суть |
|---|---|---|
| **Двойной счёт 17 924,46 ₽** | строки 2497 и 2501 | товар строки 2497 учтён ещё раз внутри 2501; помечено вашей же припиской «записаны в строку 2501». Из-за этого платёж 144 274,69 ₽ не привязывается |
| **Дубль строки** | Профлес, счёт 3509 | две строки на один счёт; на второй висит платёж 46 262,40 ₽ |
| **Две фирмы под одним именем** | Вудмарт ООО | счёт № 893 на 26 000 ₽ принадлежит ИП Кожухову В. К., остальные пять — ООО «ВУДМАРТ» |
| **Крупные плательщики без карточки** | Терскол ООО — 7 306 972 ₽, ДТРД ООО — 1 377 000 ₽ | поступления есть, покупателя в файле нет |

---

## 5. Порядок загрузки

1. Нормализовать файл.
2. **Прогнать проверку вхолостую** — она сверит паспорта и покажет, что
   опознается, что заведётся заново и что оборвётся. В базу не пишет ничего:

   ```
   ssh VPS "sudo -u finbg /opt/finbg/venv/bin/python \
     /opt/finbg/imports/check_buyers_reimport.py /opt/finbg/imports/новый_файл.json"
   ```

3. Посмотреть на список обрывов, поправить файл, прогнать снова.
4. Заливать, когда список пуст или каждая строка в нём понятна.

Сегодня файл нужно предварительно превратить в JSON — этим занимаюсь я. Чтение
xlsx прямо на сервере пока не сделано, это отдельная работа (пункт 5 в `PLAN.md`).

---

## 6. О чём знать заранее

**После перезаливки «оплачено» будет считаться по привязанным платежам, а не
браться из файла.** Расхождение на сегодня:

| | |
|---|---|
| Оплачено по файлу | **65 628 074,64 ₽** |
| Оплачено по привязанным платежам | **46 995 126,23 ₽** |
| Разница | **18 632 948,41 ₽** |

Эти 18,6 млн переедут из «оплачено» в «не оплачено». Деньги не пропадают: это
14 сделок без привязанных платежей плюс оплаты 2025 года, которых в базе нет
вовсе — загружены поступления с 12.01.2026.

Месяц назад разрыв был 25 млн, работа по привязке сократила его до 18,6. Перед
заливкой это надо либо дочистить, либо принять сознательно.

---

## Что уцелеет при перезаливке

Накопленное в базе, чего в Excel нет:

| | |
|---|---|
| Связей «платёж → сделка» | 282 |
| Клиентов с ИНН | 106 из 121 |
| Счетов из Bitrix с валютой | 217 |
| Альтернативных написаний названий | 1 |

Счета Bitrix, ИНН и привязка менеджеров переживут перезаливку без вопросов —
они не зависят от сделок. **Связи «платёж → сделка» зависят**: они и есть то,
что защищает паспорт сделки и проверка вхолостую.
