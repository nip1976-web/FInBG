# План работ FinBG

Состояние на 30.07.2026.

## Первый этап: свести деньги со сделками

### 1. Спецификации в автопривязке

Автоматическая привязка платежа к сделке умеет находить в назначении платежа
номер **счёта**, но не номер **спецификации**. Из-за этого 28 сделок из 283
стоят без привязанных платежей, хотя деньги на счёт пришли и лежат в базе.
Самые крупные: ПИК ЛЕС (5,2 млн), Приангарский (5,2 млн), Павловский ДОК
(2,1 млн).

Сделать: научить разбор назначения платежа понимать «СП №55», «спецификация 61»
и подобное, привязать эти 28 сделок, спорные вынести на ручную проверку.

### 2. Включить расчёт итогов по привязкам

Готово и лежит непримененным: `api/sql/012_deal_totals_from_allocations.sql`.
«Оплачено» и «остаток» перестают браться из excel-выгрузки и считаются по
привязанным платежам; триггер держит их в порядке при любой привязке, отвязке и
переносе платежа. Все четыре пути проверены на живых данных.

Только **после** пункта 1: сейчас включение увело бы 25 млн ₽ из «оплачено» в
«не оплачено» (было 65,6 млн оплачено и 28,1 млн остаток, стало бы 40,6 и 53,1).

Внимание: новый загрузчик уже содержит этот пересчёт, поэтому **следующая
перезаливка файла включит его сама**. Перед перезаливкой предупредить.

### 3. Три платежа на ручной проверке

Вкладка «Входящие Payments», галочка «Только расхождения»: две доплаты ЛДК № 3
по старым счетам 179 и 206, и платёж «Дом». У последнего счёт верный, но в
карточке компании в Bitrix вместо названия стоит «ываыв» — правится на стороне
Bitrix, силами Николая.

### 4. Сделка 174 (Профлес)

Дубль строки в файле. По счёту 3509 два платежа: 46 262,40 ₽ и доплата
7 145,60 ₽, вместе 53 408 ₽. По-хорошему платёж 307 перенести на сделку 166 —
тогда счёт закрывается ровно, — а пустую сделку 174 убрать. Проверено: перенос
даёт верную картину. Требует решения Николая, потому что удаляет строку.

### 5. Кнопка загрузки Buyers.xlsx

Финал первого этапа. Сейчас загрузку данных делает не Николай: кнопки в
интерфейсе нет, загрузчики запускаются командой на сервере и принимают уже
разобранный файл, а не xlsx.

Сделать: чтение xlsx на сервере, кнопка на странице «Черновик импорта», показ
того, что изменится, до записи в базу, подтверждение. Оценка — день работы.
Строго после пунктов 1 и 2, иначе кнопка одним нажатием уводит 25 млн в
«не оплачено».

## За рамками первого этапа

### Вход через Google

Упирается не в код, а в две вещи со стороны Николая: домен (на голый
IP-адрес сертификат не выпустят, а Google Sign-In требует HTTPS) и OAuth
Client ID из Google Cloud Console. Разрешённый вход пока один: nip1976@gmail.com.

## Сделано 30.07.2026

- **Опечатка в номере счёта.** Сделка 138 (Новоторжский ЛПК, 11 000 ₽): счёт
  3229 заменён на 3122. 3229 принадлежит Алтай-Форесту. Правило «номер счёта
  уникален и принадлежит одному клиенту» проверено по всей базе.
- **Сделка опознаётся по документу, а не по номеру строки Excel.** Раньше
  удаление строки из файла сдвинуло бы все строки ниже, и при перезаливке данные
  приехали бы в чужие сделки. Формула ключа сверена: 283 из 283.
- **Отчёт о пропавших сделках** после загрузки — с клиентом, суммой и числом
  привязанных платежей. Ничего не удаляет.
- **Все платежи по сделке.** Таблица показывала только первый платёж, а клиенты
  платят авансом и потом расчётом. Теперь видно число платежей, первую и
  последнюю дату; щелчок по графе «Оплачено» открывает список.
- **Одинаковое меню** на всех страницах; нерабочие разделы помечены «скоро» и
  больше не нажимаются.
